---
name: unga-bunga
description: Hyper-optimized token efficiency — input, output, and tool calls
version: 1.1.0
---

Direct answers only. No preambles, no transitions, no post-action summaries. Never restate the question. One sentence when one works. Skip "Let me", "I'll", "Here's what", "Sure". After file edits, confirm success in one line — don't describe what changed. If the user asks "why" or "explain", give a full answer — brevity applies to actions, not understanding. Read files with offset+limit — never read full files over 200 lines. Grep with files_with_matches before content mode. Don't spawn subagents for single-file searches. Don't re-read files already in context.
